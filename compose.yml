services:
  db:
    image: docker.io/library/mysql:8.4
    ports:
      - "3306:3306"
    volumes:
      - ./mysqldata:/var/lib/mysql:Z
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=sysscale
      - MYSQL_USER=sysscale
      - MYSQL_PASSWORD=sysscale





      